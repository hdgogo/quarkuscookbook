package org.example.app.security.jpa;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativePublicResourceIT extends PublicResourceTest {

    // Execute the same tests but in native mode.
}